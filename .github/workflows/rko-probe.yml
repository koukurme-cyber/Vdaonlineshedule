name: Monitor RKO group changes (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "RKO collector is disabled to avoid load on adultchildren.ru"
